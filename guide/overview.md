# 交付概览：看板新手使用说明与操作指引（真实截图版）

## 做了什么
为「供应链在途数据看板」写了一份**面向零基础新手**的图文使用说明，**按 6 个页面逐页讲解**。按你的反馈做了两项关键升级：

1. **图片全部换成看板真实截图**（本机装了 headless Chromium 后实拍 `index.html`），并用红色编号 ①②③ 直接标注在**真实界面元素**上——不再是示意图，字体、配色、布局与原网页**完全一致**。
2. **操作指引改成"编号步骤"**：正文每一步的 ①②③ 与截图上的编号一一对应，照着编号点就行。

## 交付物（`supply-dashboard/guide/`）
| 文件 | 说明 |
|---|---|
| `使用说明.html` | 单文件 HTML 预览（截图已内联/引用，双击即看） |
| `使用说明.md` | 指引正文源（转 PPT / PDF / 在线文档用） |
| `imgs/*.png` | 11 张真实截图（2x 高清，带编号标注） |
| `shoot.mjs` | 截图脚本（页面改动后可重跑，重新生成全部截图） |
| `imgs/_schematics/*.svg` | 首版矢量示意图（备用，可做 PPT 素材） |

## 截图清单（11 张）
`overview-full` 整体界面 · `filter` 筛选条特写 · `month` 月份开关特写 · `page-overview` 在途概览 · `drawer` 下钻抽屉 · `page-supplier` 供应商分析 · `page-capacity` 产能负荷 · `page-weekly` 周数据计划 · `page-overdue` 逾期分析 · `page-cross` 交叉分析 · `table` 表格排序/列筛选特写

## 内容结构
30 秒了解 → 整体界面（编号对照表）→ 通用操作（筛选条 / chips / 本月下月 / 两级下钻抽屉 / 表格排序筛选）→ **逐页指引（6 页）** → 操作速查表 → FAQ。

## 关键口径（正文已写明）
- 负荷率 = 该月在途(剩余收料) ÷ 供应商月产能（不跨月累加）
- 逾期 = 交期 < 今天 且 剩余收料 > 0；完成率 = 已收料 ÷ 采购数量
- 逾期分析、交叉分析**刻意**不跟随本月/下月开关（全量 / 双月对比）

## 后续
- 呈现形式任选：HTML（现成）、MD 转 PPT/PDF、或只用截图自由排版。
- 数据更新后重跑 `shoot.mjs` 即可让全部截图同步最新（需 headless Chromium，脚本与用法已存入技能 `dashboard-usage-guide`）。
